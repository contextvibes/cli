/*
Package scaffold provides the provider for loading embedded scaffold templates.
It abstracts the file system access to the embedded assets directory, allowing
commands to retrieve configuration templates for various targets (e.g., idx, vscode)
without hardcoding strings in the source.
*/
package scaffold
