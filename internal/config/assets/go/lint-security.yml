version: 2
run:
  timeout: 5m
linters:
  disable-all: true
  enable:
    - gosec
    - errcheck
    - govet
    - staticcheck
    - bodyclose
    - noctx
    - rowserrcheck
    - sqlclosecheck
    # Do NOT include 'typecheck' here, it causes a crash in modern golangci-lint
linters-settings:
  gosec:
    excludes:
      - G101 # Hardcoded Credentials (handled by gitleaks)
      - G304 # File inclusion (CLI tool nature)
  errcheck:
    exclude-functions:
      - (*os.File).Close
