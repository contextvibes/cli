# Generates a system prompt for a target environment.

Builds a complete system prompt by combining a universal 'core.md' with a
target-specific instruction file (e.g., 'idx.md') from the internal
prompt library.
