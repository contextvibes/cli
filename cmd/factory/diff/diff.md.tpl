# Shows pending Git changes and saves them to a file.

Generates a Markdown summary of pending changes (staged, unstaged, untracked)
in the Git repository and OVERWRITES the context file: contextvibes.md.
