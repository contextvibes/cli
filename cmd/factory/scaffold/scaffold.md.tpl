# Scaffolds infrastructure and project configuration.

Initializes the backing services and configuration for the project.
Currently supports:
- **idx:** Generates a standard .idx/ directory with dev.nix, contextvibes.nix, and local.nix.
- **firebase:** Wraps 'firebase init' to generate firebase.json, .firebaserc, and security rules.
