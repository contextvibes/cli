# Run project tests.

Detects the project type (Go, Python, etc.) and executes the appropriate test runner.
Arguments passed to this command are forwarded to the underlying test tool (e.g., `go test`, `pytest`).
